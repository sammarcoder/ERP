'use client'

import React from 'react'
import UnifiedOrderForm from '@/components/UnifiedOrderForm'

const PurchasePage = () => {
    return <UnifiedOrderForm orderType="purchase" />
}

export default PurchasePage
